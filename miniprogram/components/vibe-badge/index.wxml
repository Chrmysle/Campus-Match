<view class="vibe-badge vibe-badge-{{size}} {{active ? 'vibe-badge-active' : ''}}" bindtap="onTap">
  <text wx:if="{{emoji !== ''}}" class="badge-emoji">{{emoji}}</text>
  <text class="badge-text">{{text}}</text>
</view>